# Java protobuf hello world

Run the Maven command bellow to generate the protobuf classes.

```java
mvn clean package
```

Then go to the package **com.mqs.com** and run the main method of the classes Writer and Reader to see the results.
